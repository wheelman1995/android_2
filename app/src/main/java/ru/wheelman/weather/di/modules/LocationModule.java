package ru.wheelman.weather.di.modules;

import toothpick.config.Module;

public class LocationModule extends Module {
    public LocationModule() {
//        bind(LocationHelper.class).toInstance(new LocationHelper());
    }
}
